---
apiVersion: batch/v1
kind: Job
metadata:
  generateName: flow-otg-otg-
  labels:
    flow: otg-otg
spec:
  backoffLimit: 1
  template:
    spec:
      containers:
        - command:
            - /bin/sh
            - "-c"
            - |
              curl -L https://raw.githubusercontent.com/raballew/kne-on-ocp/main/flows/flow-otg-otg.yaml > otg.yaml
              otgen run --file otg.yaml --api https://service-https-otg-controller --insecure --metrics flow | otgen transform --metrics flow --counters frames | otgen display --mode table
          image: quay.io/pwallrab/otgen:0.1.0
          name: otgen
      restartPolicy: Never
